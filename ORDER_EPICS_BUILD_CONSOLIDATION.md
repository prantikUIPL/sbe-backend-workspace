# Order Epics — Remaining Work Consolidation & Reuse Map

Post-ship rewrite. **13.1/13.2/13.3 SHIPPED** (exhibitor-backend-api, SBE-1146 → dev, PRs #272/#276) and **24.1–24.4 SHIPPED** (admin-backend-api, SBE-1125 → dev, PR #513). **24.12 and 24.13 are OUT OF SCOPE this sprint.** Purpose shifts accordingly: this doc now answers *"what remains (24.5–24.11, 24.14, 24.15) and how to build it with the least work by reusing what is on dev."* Shipped work is collapsed into reuse-target blocks; live clusters keep their old ids. Source of truth remains each story's feasibility `.md`.

## Shipped — reuse targets on dev

### Order History (Epic 13) — exhibitor-backend-api, `src/orders/`
Two routes only (no invoice/agreement/schedule sub-routes exist; swagger declares them intentionally absent — `orders.controller.ts:102-103`):

- **GET /api/v1/orders** (13.1+13.2) — company-scoped paginated list, all order types incl. pending, derived payment_status/is_overdue/can_pay/shows[] (`orders.controller.ts:57,64`; `orders.service.ts:65,92-103`)
- **GET /api/v1/orders/:orderId** (13.3) — ownership-404 aggregate: header, booths/add_ons/sponsorships, financial_summary, settled-only payments[], agreement block (`orders.controller.ts:90`; `order-details.service.ts:101-107,147-167`)

| Reuse target | Where | Reused by |
|---|---|---|
| `deriveOrderPaymentStatus` / `deriveIsOverdue` / `deriveCanPay` / `buildOrderShows` (pure) | `src/orders/orders.helpers.ts:53,74,86,111` | 24.7 Paid/Unpaid representation (derive, don't add a column), 24.8 mapper status, 24.15 overdue selection — copy cross-repo |
| Hardcoded status sets `SETTLED={succeeded,refunded,canceled}`, `NON_PAYABLE={canceled,refunded}` | `orders.helpers.ts:31,34` | **Watch item:** one-line extension each when 24.9 `refund_failed` / 24.8-l chargeback statuses land, else refund-failed orders render payable |
| `classifyOrderItems` (cart_item_type + parent_order_item_id folding), `formatBoothSize`, `buildFinancialSummary` (null-when-zero), `buildPaymentRecords` (succeeded-only; pm lookup w/o deleted_at filter), `buildAgreement` | `src/orders/order-details.helpers.ts:104-143,37,187-202,240-252,281-289` | 24.6 detail aggregate (pattern-level; columns already in admin via migration `20260618120000_add_order_item_tree_and_cart_type`) |
| Ownership-404 + frozen SELECT-constant discipline (`findFirst {id, company_id, deleted_at:null}` → 404) | `order-details.service.ts:101-107`; `src/common/helpers/resolve-exhibitor-company.helper.ts:11` | Every OM `/orders/:id/*` route (RBAC-scoped instead of company, same 404/select discipline) |
| coupon_code via `cart.coupon.code` join (Order.coupon_code unpopulated at checkout) | `order-details.service.ts:39,163` | 24.6 must do the same join or fix the write-path |

**Not shipped from 13.x** (details in dependency tables below): 13.2-g/13.3-s invoice PDF (deferred → A-vs-B open decision); 13.3-t schedule, -l gift cert, -v agreement download, -w onsite contact, -g upgrade (blocked); 13.3-c booth number, -f booth type (not deliverable). 13.2-h Partial: can_pay shipped, Invoice flag deferred with 13.2-g.

### Order Management 24.1–24.4 — admin-backend-api, `src/admin/orders/`
One consolidated endpoint **GET /api/v1/orders** (list+filters+search+sorting; former cluster **C1 — RESOLVED-SHIPPED**, zero residual incl. 24.1-n/24.2-c/24.2-e resolved Deliverable 2026-07-02). Unshipped: 24.1-b booth-build rows (Blocked, no producer — SBE-1304/1399/1088; list auto-surfaces them if they land as `order_type=product`); 24.1-e Quick Links → 24.5; 24.2-d city dropdown → existing GET /api/v1/cities.

| Reuse target | Where | Reused by |
|---|---|---|
| **The module itself** — `@Controller('orders')`, OrdersModule, DTO/envelope conventions, Swagger tag | `src/admin/orders/orders.controller.ts:22-30`; `orders.module.ts:1-13` | ALL remaining `/api/v1/orders/:id/*` routes (24.5–24.10, 24.14) are **additive routes on this module** — the feasibility docs' "no order module exists anywhere" blocker is stale |
| `orders.list` permission precedent (seeder 'orders' block + role grants) | `src/database/seeds/permission.seeder.ts:1205-1208`; `role.seeder.ts:178` | Every new per-action key (`orders.cancel`, `orders.refund`, `orders.notes.*`, …) = appended seeder rows + one `@Permissions()` decorator |
| `deriveBalanceDue` (Decimal minus, floored 0, fixed-2 string) + `ORDER_LIST_SELECT`/`toListItem`, structural-typed helpers (`buildOrderShows`, `buildCustomerName`, `buildSalesPersonName`, `deriveSalesChannel`) | `orders.helpers.ts:33-36,43-113`; `orders.service.ts:24-54,172-189` | 24.6 Order Total / Balance Due / shows / names FREE; 24.7-e, 24.8-h unallocated balance (same fixed-2-string money convention) |
| `buildCreatedAtRange` (calendar-invalid rejection, UTC bounds, from≤to 400) + `successApiResponse`/`ApiResponseDto` envelope | `orders.service.ts:142-171`; `src/common/utils/api-response.util.ts:8` | Any remaining date-filtered/list-shaped sub-resource |
| `PdfmakeService.render` — general PDF renderer (pure-JS pdfmake, no Chromium) | `src/common/helpers/pdfmake.service.ts:27,53` | 24.5-f signed-contract PDF (net-new composition, decided 2026-07-03), any other PDF output in remaining OM stories (24.5-g invoice uses pdfkit per the ppl-order port precedent) |

## Remaining OM stories — clusters (updated)

**Build-first owners (4): 24.9, 24.10, 24.14, 24.8** (+24.5 owns C6). **C1 shipped. C12 dissolved** — both members (24.12, 24.13) out of scope; its shared unit (reservation release) is reassigned into C2/24.10 below.

### C2 — Order cancel endpoint: cancellation + inventory release + email toggle
*owner **24.10** · absorbs former C12's release step*

- **Unit:** POST /api/v1/orders/:id/cancel on the shipped orders module — one `$transaction`: validate (order_type=product, deleted_at null, not canceled) → `Order.status=canceled` → `paymentTransaction.updateMany` scheduled/unpaid→canceled + `next_retry_at:null` (copy webhook pattern at `external-api-service/.../webhook.service.ts:~2890-2896, ~2990-2996`) → `inventoryReservation.updateMany {order_id, status:committed}→{status:released, released_at}` → `AdminAuditService.record(order, tx)`.
- **Release step is mandatory in 24.10 even though 24.12 is out of scope:** reservations are created `committed` with `order_id` at checkout (admin `inventory.service.ts:117-128`; exhibitor `cart.helpers.ts:140-165`), all availability math sums committed rows with no expiry, and the ONLY release writers anywhere are the two external-api webhook payment-failure paths (`webhook.service.ts:2903-2908, 3001-3006` — old citation 2762-2765 has drifted to PDF code). A cancel that skips release strands the units forever. Cost: ~5 lines, no lock needed. House as `InventoryService.releaseForOrder(orderId, tx)` (admin `inventory.service.ts` has getAvailability:53 / assertAvailable:81 / commitForCart:104 but **no release method**) so 24.13 reuses it next sprint.
- **Refund legs stay gated on 24.9** (C3): still no Refund model, no `refund_failed`, no outbound `stripe.refunds.create` anywhere. Minimal C2 unit = cancel-without-refund-execution.
- **Members/residuals:** `24.10` (owner) — the transaction above; 24.10-d/f/h blocked on 24.9; 24.10-g blocked on manual-payment representation (24.7); 24.10-j OOS. `24.11` (rider) — `send_notification` boolean (default true) on the cancel DTO + in-process dispatch (C4). **Deferred with 24.12/24.13:** manual-floorplan reminder flag (24.12-e/f), Social Tables auto-release (24.12-d), move-show re-mapping (24.13 — will call `releaseForOrder`).

### C3 — Order refund endpoint + Stripe wrapper (absorbs former C7)
*owner **24.9***

- **Unit:** (1) Prisma migration: `Refund` model + `refund_failed` enum value — re-verified absent (`grep 'model Refund'` / `refund_failed` = zero in schema; admin owns the migration). (2) Two additive routes on the shipped module: GET /api/v1/orders/:id/refund-options (eligibility read off `Order.stripe_charge_id` schema:1566 / `PaymentTransaction.stripe_charge_id` schema:2078 — columns already populated, "Stripe only if paid via Stripe" is a column read) and POST /api/v1/orders/:id/refunds (cap = paid_amount − prior Refund rows via the `deriveBalanceDue` Decimal pattern, mandatory reason, manual-vs-Stripe branch, tx-joined audit, `send_notification` flag). (3) **external-api-service POST /v1/refunds** internal wrapper (`stripe.refunds.create`) — still zero refund controllers/calls in any repo.
- **Idempotency constraint (new):** the existing `charge.refunded` webhook handler (`webhook.service.ts:3016-3049`, handler :3022) will fire after every wrapper refund and amount-blindly flips the PT to `refunded` + voids its invoice — 24.9's synchronous transitions must be idempotent against it; partial-refund accounting cannot rely on PT.status alone (the Refund model is the ledger).
- **Members/residuals:** `24.9` (owner) — above; 24.9-f manual-only-for-offline stays Partial (no offline-payment write path); 24.9-i QuickBooks OOS. `24.6` [24.6-z] — refund half FREE once 24.9 ships (detail action calls the endpoint); void half stays Blocked, separate. `24.10-f` — wire the wrapper call into the cancel tx; 24.10-h unblocks the moment 24.9's **migration** merges (sequence on the migration, not the full endpoint). `24.11` — refund-email toggle (C4).

### C4 — Email dispatch & the 24.11 toggle (absorbs former C11)
*owner **24.11** for the toggle; primitive already built in BOTH repos*

- **Correction:** admin-triggered action emails (24.5-i/j, 24.6-r, 24.11) use **admin-backend-api's own** `MailerService.sendFromTemplate` (`src/common/services/mailer.service.ts:192`, notification_logs write :242, non-throwing) — NOT background-worker's mailer, which applies only to 24.15's cron leg (`background-worker-service/src/notification/mailer.service.ts:88`).
- **Verified absent on dev:** NO order-cancellation / refund / payment-reminder trigger_events or templates (trigger-event.seeder.ts has 26 slug entries — its header comment stale-says 21 — nearest is `ppl_subscription_canceled` :104; the 40-template Email&SMS set is on its unmerged branch — coordinate slugs to avoid collision).
- **Members/residuals:** `24.11` — seed 2 trigger_event slugs + 2 templates (the only genuinely net-new artifact) + `send_notification` flag on the 24.10/24.9 DTOs with conditional in-process dispatch; ~half-day once 24.9/24.10 exist. `24.5` [24.5-i/j] — **DONE 2026-07-03** (`OrderActionsService` on `feature/SBE-1125`, admin `3624d09`): ppl-order resend stack ported with review hardening — SENT-only throttle, literal token rendering (not Handlebars), portal-password delivery verified via this-send's log. ⚠ the shipped ppl-order/cart twins still carry the pre-fix behaviors (see the 24.5 doc's review register). `24.6` [24.6-r] — POST :id/send-email handler: template dropdown = notificationTemplate list, dispatch = admin sendFromTemplate; net-new is handler+DTO only. `24.15` [24.15-c/d] — worker leg, see C13.

### C5 — Notes sub-resource: 24.14 owns, 24.6 reads
*owner **24.14** (SBE-1138, Prantik) — **SHIPPED: merged to dev 2026-07-03** (admin PR#516 `09a652d`, exhibitor #284, bg-worker #25, external #88, pulse #14 — pipelines + Sonar all green). The 4 Order note columns + `GET/PATCH /orders/:id/notes` are now live reuse targets on dev; 24.6-c/e/f flipped Partial → Deliverable and 24.6-d Not Deliverable → Deliverable (display-only residual) in the 24.6 doc 2026-07-03*

- **Unit:** one migration adding **four** nullable columns to Order — `internal_notes Text`, `payment_memo Text`, `invoice_note VarChar(255)`, `additional_terms Text` (invoice_note/additional_terms copy Cart defs verbatim, schema:2853-2854) + GET/PATCH `:id/notes` on the shipped module. PATCH = one `$transaction` of write + per-field `AdminAuditService.recordMany`, copying the proven `account-history-notes.service.ts:25-32` template. Seed `orders.notes.read/update`; permission keys documented in the routes' Swagger docs.
- **⚠ `Order.notes` is RESERVED — do not admin-expose or overwrite it (any story):** it is the exhibitor checkout **idempotency-key store** — both exhibitor checkout paths write `idempotency_key:<key>` into it at order creation (`cart.service.ts:1277`, `payments.service.ts:213`) and substring-match it (`notes: {contains}`) for 24h duplicate-order/charge replay protection (`cart.service.ts:1541`, `payments.service.ts:465`). That's why 24.14-a adds `internal_notes` instead of reusing `notes` (the prior "reuse Order.notes" assumption was wrong). Longer-term hygiene idea (unowned): relocate the key to a dedicated column — cross-repo exhibitor refactor, not part of any current story.
- **Cart-seeding RESOLVED 2026-07-02:** the new columns are created **blank** at checkout (admin-entered on the Notes tab); no checkout write-path changes in any repo.
- **Audit trail display is ZERO work:** shipped GET /api/v1/logs/admin-audit (`admin-audit-log.controller.ts:17,30`) already accepts `entity_type=order&entity_id=:id` (`IsEnum(AdminAuditEntityType)`, `order` in enum schema:2205).
- `24.6` [24.6-c/d/e/f] — read-only fields in its detail aggregate (select the four Order columns); edits DELIVERED by 24.14's PATCH; **24.6-d flips Not Deliverable → Deliverable when 24.14's `payment_memo` lands** (update the 24.6 doc then).

### C6 — Agreement PDF/DOCX download: 24.5 owns, 24.6-s free
*owner **24.5** (SBE-1129, Prantik) — **SHIPPED: merged to dev 2026-07-03** (PR#521 `34c6fb4`; pipeline #856 + Sonar green). Signed DOCX port + net-new signed PDF live as `OrderAgreementDocumentService`; **24.6-s is DELIVERED**. ⚠ NEW SHARED ASSET: the DOCX/PDF agreement toolchain now lives in `src/admin/common/agreement-document/base-agreement-document.service.ts` — the cart agreement service AND the orders one both extend it; future document renderers should too. Historical note: scope decided 2026-07-03 = SIGNED contract (the "ZERO renderer work" claim below assumed the rejected unsigned delegation)*

- **DECISION 2026-07-03 (supersedes the delegation plan below):** downloads serve the **SIGNED** contract. 24.5-e = port exhibitor `buildSignedBoothContractDocx` into the orders module (admin ppl-order's signed-agreement service is the proven port template; cache-first on DB-shared `OrderAgreement.signed_agreement_url`). 24.5-f = **net-new signed-PDF pdfmake composition** (no signed-PDF renderer exists anywhere; DOCX-only cache — PDF caching decided at plan time). The unsigned cart generator stays untouched as the cart-side draft renderer.

- **Corrections:** `utils/pdf.util.ts` no longer exists — PDF is pdfmake via `PdfmakeService.render` (`src/common/helpers/pdfmake.service.ts:27,53`), DOCX via html-to-docx; and the cart generator does **not** read OrderAgreement — `AgreementDocumentService.generate(cartId,'pdf'|'docx',performedBy)` (`agreement-document.service.ts:104`) renders the UNSIGNED agreement from live cart context (signed branch :336-353 exists uncalled). Real data caveat is `order.cart_id` (nullable FK, soft-deleted cart) → 404/fallback, not OrderAgreement.
- **Unit:** two thin routes on the shipped orders module — GET :id/agreement.pdf/.docx — resolve order → cart_id, delegate to the unchanged cart generator, copying the streaming pattern at `cart.controller.ts:1297-1345` (real `.docx`, StreamableFile). NOTE: the generator's built-in download audit records against the **cart** entity (`agreement-document.service.ts:155-173`) — add an order-entity `AdminAuditService.record` in the delegating route if order-level audit is wanted. One-line `CartModule` exports addition (`cart.module.ts:46`) + permission seed.
- **Exhibitor-mirror question ANSWERED:** rejected for 24.5-e/f — the exhibitor signed-agreement route is cross-repo and DOCX-only (fails 24.5-f; see 13.3-v reconciliation row). If a SIGNED booth document is required, admin's ppl-order signed-agreement service (`src/admin/ppl-order/services/agreement-document.service.ts:29-66`, cache-first on DB-shared `OrderAgreement.signed_agreement_url` schema:1622-1627, exhibitor pre-warms the cache) is the cheap template; only a signed PDF would be new pdfmake work.
- `24.6` [24.6-s] — NO build; consumes 24.5's routes.

### C8 + C9 + C10 — cross-cutting, now near-zero
- **C8 RBAC:** mechanism fully shipped and proven (global guards `app.module.ts:86-91`, `roles.guard.ts:49-70` w/ Developer bypass; `@Permissions` = SetMetadata `permissions.decorator.ts:3-4`). 24.1–24.4 residuals DONE; 24.12-f/24.13-h dropped (OOS). Remaining per story = seeder rows + decorators; substantive leftovers are only the domain gates 24.7-d / 24.8-m / 24.9-h (24.5's actions-GET was DROPPED 2026-07-03; its 6 per-route keys SHIPPED with `3624d09`). ⚠ NEW convention from 24.5's review: id params use `RawParam`+`ParseIntIdPipe` (hex/INT4-overflow guard) — the orders module is now fully converted; future OM routes should start there, not `ParseIntPipe`.
- **C9 Audit:** primitive fully built — tx-aware `record`/`recordMany` (`admin-audit.service.ts:30,50`; throws on blank `note`), provided by `AdminAuditModule` (`admin-audit.module.ts:5-6`), `order` already in `AdminAuditEntityType`, read endpoint shipped. Residual per mutating route (24.7-c, 24.10-i, 24.14-e, 24.8 mutations, 24.9-h) = one module import + one `record()` call inside the tx.
- **C10 Money scalars:** VERIFY item answered from code — key `orders.list`; `item_total = total.toFixed(2)`, `balance_due = deriveBalanceDue` (`orders.service.ts:181-182`; `orders.helpers.ts:33-36`), fixed-2 strings off Order.total/paid_amount only. 24.1 residual DONE (24.1-n resolved Deliverable 2026-07-02 — stale "Partial" dropped). Remaining: 24.6 imports the helpers (Order Total / Balance Due FREE, same string convention); only genuinely new work is **24.6-o's fee/items-subtotal breakdown** over OrderItem.

### C13 — PaymentTransaction installment-row mapper: 24.8 owns; 24.7, 24.6-p/q, 24.15 reuse
*owner **24.8** (SBE-1132, Prantik) — mapper still fully net-new (zero non-spec `paymentTransaction` refs in admin src)*

- **Unit:** 4 sub-routes on the shipped module (GET :id/payment-plan; POST/PATCH/DELETE :id/payment-plan/installments[/:installmentId]) + one shared PT→installment-row mapper (installment_number, amount, status, due_date, paid_at, Invoice# via `invoice_id`→`Invoice.invoice_number` schema:1972,1976, amount-due via `deriveBalanceDue`, next-installment = earliest non-settled due_date per the exhibitor SETTLED-set pattern).
- **Data facts (verified):** PT model schema:2067-2103 w/ `[status,due_date]` index; NO plan linkage — CartPaymentPlan/ScheduleEntry are display-only via `Order.cart_id` (plan comment "validation/display only"); checkout writes PTs independently (exhibitor `payments.service.ts:257-285`, one row per installment). `CartPaymentScheduleEntry.method` enum (`credit_card|bank_wire_ach|check|paypal`, schema:2795-2802) exactly matches 24.7's manual-method taxonomy → usable classification fallback today. `PaymentTransactionSource` now has a 4th value `subscription_renewal` (schema:503-509) — include in exhaustive switches.
- **24.8 residuals:** migration adding `payment_type` + `payment_memo` to payment_transactions (24.8-a Partial until then); relax NOT-NULL stripe cols for manual installments (24.8-f); port the shipped 60/30 warn-not-block util + `?confirm=true` bypass (`cart-payment-plan.util.ts:8-11,52-78`; `cart.controller.ts:1230-1260`); 24.8-d status-flip only (money → 24.9); 24.8-l stays Blocked (no QuickBooks/chargeback code).
- **24.7:** shrinks to GET :id/payments (reuse mapper) + PATCH :id/payment-status w/ eligibility gate + audit; classification via cart-plan `method` path (24.7-a closer to Deliverable than the doc's Partial if the team accepts that path; true per-installment type needs 24.8-a's column). **Mark-as-Paid ownership corrected: it is 24.8's installment PATCH, not 24.7** — 24.15's stop-condition gates on BOTH.
- **24.6-p/q:** free once the mapper exists — embed its output.
- **24.15:** 24.15-c/d/h deliverable NOW: clone the payment-charge registrar/job pair (that registrar registers only two crons — 02:00 charge, 06:00 retry; NO reminder cron in any of the five scheduler registrars, zero 'reminder' hits in worker src) into a reminder cron scanning PT via the `[status,due_date]` index (overdue = mirror `deriveIsOverdue`; upcoming = scheduled within window), send via worker `mailer.service.ts:88`, seed upcoming/overdue templates (net-new), add one handler to the existing `manual-trigger.controller.ts` (:26,:55 precedents). Stop-condition needs no new state — checking PT.status at send time auto-stops once 24.8's PATCH ships; 24.15-a/b/e/f/g stay Blocked on 24.7/24.8's manual lifecycle. Build order: 24.8 → 24.7 → 24.6-p/q → 24.15 unblock (cron+templates in parallel).

## Open decisions (recorded, not owned)
- **Invoice semantics A-vs-B** (13.2-g / 13.3-s / 13.3-ac invoice half / 24.5-g): **RESOLVED for 24.5-g 2026-07-03 = B** — serve persisted webhook-created Invoice rows, per-invoice route mirroring the shipped PPL shape (`GET /orders/:id/invoices/:invoiceId/invoice`, cache-first on `Invoice.invoice_pdf_url`; split→N is handled by the per-invoice shape, unpaid→0 invoices simply don't exist yet). Booth/product PDF layout is net-new over the typed line-item tree. The 13.x exhibitor-side question stays open for that epic's owner.
- **Cancel-without-refund-execution tradeoff** (24.10): ship cancel with refund amount recorded intent-only, or sequence 24.9 (at minimum its migration) first to avoid a re-open.
- **Refund unit** (24.9): order-level vs per-installment — gates coding.
- ~~**Agreement variant** (24.5-e/f)~~ **RESOLVED 2026-07-03: SIGNED contract** — port the exhibitor booth builder via the ppl-order cache-first template (DOCX) + net-new signed-PDF pdfmake composition. See C6.
- ~~**24.14 column seeding**~~ **RESOLVED 2026-07-02:** leave the new Order note columns blank at checkout (admin-entered on the Notes tab; Cart columns stay untouched as cart-level fields).
- **Pending-order inclusion** (13.1/13.2): shipped as included; keep / age-filter / exclude is a single where-predicate change.
- **13.3-t option (b):** a PT-based schedule read is deliverable today (checkout writes per-installment rows) if the team wants it before 24.8/SBE-1154.
- **13.3-v signed agreement:** see reconciliation row below — confirm with Kshitiz, reconcile SBE-1158.

## Inter-Story & Cross-Epic Dependencies

**Status legend:** ✅ available · 🔵 Blocked (a real story would unblock) · ⛔ Not Deliverable (no data/story) · ⁇ open question · 🔗 already delivered / reuse.

_Last updated 2026-07-02 from post-ship re-verification (13.x + 24.1–24.4 on dev). All still-open rows below were re-verified against current dev trees._

### Order History ↔ Order Management

| Consumer (story · req) | Depends on | Provider (story · Jira) | Status | Notes |
|---|---|---|---|---|
| **13.3-t** Payment Schedule | salesperson plan **written + linked to Order**, then shown to exhibitor | OM **24.8** (producers SBE-1109/1111/1112, Sprint 5); exhibitor visibility [SBE-1154](https://unifiedinfotech.atlassian.net/browse/SBE-1154) (backlog, Prantik) | 🔵 | Re-verified: `CartPaymentPlan`/`CartPaymentScheduleEntry` cart-only, zero src consumers (schema-only, exhibitor schema:2865,2880). Option (b) confirmed deliverable now (PT rows written per installment, `payments.service.ts:261`). |
| **13.3 ↔ 24.6** aggregate shape | shared order-aggregation pattern + installment projection | 13.3 (shipped) → 24.6 | 🔗 | Upgraded from ⁇: pattern-level reuse only (separate repos) — mirror 13.3's aggregate/`classifyOrderItems`/settled-only payments; prerequisite columns already in admin via migration `20260618120000_add_order_item_tree_and_cart_type`. Installment projection stays with OM-C13/24.8. |
| **13.2/13.3** lifecycle display | NEW enum values from OM write-side | OM **24.9** (`refund_failed`), **24.8-l** (chargebacks) | ⁇ watch-only | Narrowed: shipped read-side already handles canceled/refunded correctly. Only exposure = extend exhibitor `SETTLED`/`NON_PAYABLE` sets (`orders.helpers.ts:31,34`) one line each when the new statuses ship. |

### External-epic blockers gating Order History (all re-verified open)

| Consumer (req) | Depends on | Provider (story / Jira) | Status | Notes |
|---|---|---|---|---|
| **13.3-l** Gift Certificate line | redemption producer (write `GiftCertificateRedeem`) | Story **12.4 "Gift Cart Redemption"** — [Exhibitor Store App](https://unifiedinfotech.atlassian.net/wiki/spaces/SBE/pages/3858137106/SBE+-+Exhibitor+Store+Application) (search "Gift Cart Redemption"); [SBE-1069](https://unifiedinfotech.atlassian.net/browse/SBE-1069) (Epic, To Do) | 🔵 | Zero producer in any repo; line omitted via conditional display. |
| **13.3-g** Booth Upgrade details | upgrade captured/linked on the order | **"Booth Upgrade"** story — [Exhibitor Store App](https://unifiedinfotech.atlassian.net/wiki/spaces/SBE/pages/3858137106/SBE+-+Exhibitor+Store+Application) (search "Booth Upgrade"); no Jira key found | 🔵 | Upgrades modelled as fresh purchases; no upgrade data persisted. |
| **13.3-c** Booth Number | stall-number assignment source, order-joined | none found. Lead: **"Booth Sign Print Page"** — [Admin Panel Page 2](https://unifiedinfotech.atlassian.net/wiki/spaces/SBE/pages/3984654414/SBE+-+Admin+Panel+Page+2) (company+show level, not order) | ⛔ | Assigned externally post-purchase (`notification-template.seeder.ts:668`); no column, no order link. Team: source downstream or drop. |
| **13.3-f** Booth Type (Std/Premium/Corner) | classification attribute | no story; data absent (`ProductType` = family only) | ⛔ | Open question: required, or BA artifact? |
| **13.3-w** Onsite Contact | `OnsiteBoothContact` populated **and** order-linked | [SBE-1169](https://unifiedinfotech.atlassian.net/browse/SBE-1169) (Sprint 5, Ritam) + producer [SBE-1165](https://unifiedinfotech.atlassian.net/browse/SBE-1165) (backlog, Manish); Edit SBE-1171. Confluence: [Exhibitor Store App](https://unifiedinfotech.atlassian.net/wiki/spaces/SBE/pages/3858137106/SBE+-+Exhibitor+Store+Application) | 🔵 | Zero producer/usage anywhere; model unused (exhibitor schema:2710), keyed `(company_id, show_id)` w/o Order FK. The Confluence Edit story intends **per-order** onsite contact, so those stories add the producer **and** the order-linkage — that's what keeps this 🔵 not ⛔. |

### Already-delivered / deferred — reconciliation

| Story · req | Reuse target / state | Status | Notes |
|---|---|---|---|
| **13.3-v / -ac** agreement half | `GET /agreement/signed/:orderId/download` (`agreement.controller.ts:154` → `buildSignedBoothContractDocx` :189) | 🔵 confirm | Route verified live on dev; authored by Kshitiz under [SBE-862](https://unifiedinfotech.atlassian.net/browse/SBE-862)/[SBE-869](https://unifiedinfotech.atlassian.net/browse/SBE-869) (Sprint 5). Zero code if confirmed final; caveat DOCX not PDF; reconcile/close [SBE-1158](https://unifiedinfotech.atlassian.net/browse/SBE-1158). |
| **13.2-g / 13.3-s** Invoice PDF | deferred by both — no product-order renderer built | ⁇ | Gated on the invoice A-vs-B open decision above; then one endpoint + the 13.2-h Invoice flag. |
| Former OH clusters C1/C2/C4/C5/C6 + OM C1 | shipped implementations (see Shipped section) | 🔗 resolved | Note: shipped OH build diverged from old cluster assumptions — net-new module using `resolveExhibitorCompanyId`/`buildOrderShows`, NOT ppl.service `listOrderHistory`/`findExhibitorCompanyOrFail`/`buildOrderGroup`. Stale line refs corrected: `computeNewOrderStatus` now `webhook.service.ts:2265` (call :2098), `paid_in_full_at` :2109. |

### Maintenance
- Update Jira/sprint status cells when tickets move; keep Confluence citations as link + heading-to-search.
- When 24.9/24.8 land new PaymentTransactionStatus values, action the exhibitor SETTLED/NON_PAYABLE watch item.
- This file lives outside the GDrive-synced `order_history/` & `order_management/` folders — dev-facing only.
