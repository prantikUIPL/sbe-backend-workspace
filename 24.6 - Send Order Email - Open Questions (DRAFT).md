# Story 24.6 — Order Details, System Specification #7 "Send Order Email"
## Open Questions for Client — DRAFT for internal review

**Status:** Draft for Prantik's review. Not a final client list — once reviewed, a trimmed client-facing version will be produced on request.
**Source:** Confluence *SBE — Admin Panel* (page `3859742741`), Story 24.6 Order Details, System Specification item **#7 of 12** (1 General Info · 2 Billing · 3 Line Items · 4 Sub-items · 5 Fees · 6 Order Totals · **7 Send Order Email** · 8 Agreement · 9 Sales Rep/Deal Owner · 10 QuickBooks Sync · 11 HubSpot Sync · 12 Permissions).
**Delivery ticket for 24.6:** SBE-1131 (delivered/merged; item #7 not part of that delivery).

---

## 1. What the spec asks (item #7, verbatim intent)

> **Send Order Email** — On the Order Details page, the admin selects an email template from a dropdown, then either saves the order or sends the email.
> Dropdown options: **New Order Confirm/Invoice · Cancelled Order · Invoice/Payment Reminder with link.**

This is a **manual, admin-triggered** email send. The complication is that two of the three options overlap with **already-delivered automated email flows**, and the third ("with link") depends on infrastructure that doesn't exist yet. Those overlaps and gaps are what need client direction before build.

---

## 2. Current build state (why these questions exist)

Investigated across all five repos + the delivered story docs. Summary:

- **No manual "send order email with a chosen template" endpoint exists.** The only on-demand order emails today are two fixed actions: `POST /orders/:id/resend-confirmation` (always the booth confirmation, invoice **PDF attached**, no template choice, throttled 5 min) and `POST /orders/:id/resend-portal-password`. There is no template dropdown, no order-scoped template-list endpoint, and no `orders.send_email` permission.
- **"New Order Confirm/Invoice"** already **auto-sends at checkout** — so the manual option is a **re-send**, not a new email.
- **"Cancelled Order"** email (`order_canceled`) exists but fires **automatically as a side-effect of the cancel/refund action** (Story 24.11, SBE-1135) — there is no standalone "just send the cancellation email" path. This flow has **no dedupe** — a manual re-send would silently deliver a duplicate.
- **"Invoice/Payment Reminder with link"** is the largest gap: the only reminder templates (`payment_reminder_upcoming` / `payment_reminder_overdue`, Story 24.15, SBE-1139) are **cron-only**, fire **only for split-payment, non-credit-card installments**, and contain **no link or PDF at all**. They do have a coarse email+slug+time-window dedupe, which a manual send could interfere with (one suppressing the other).
- **There is no signed/emailable invoice link anywhere.** The only invoice URL is a raw, **unsigned CloudFront URL** with a **guessable** key (`exports/invoices/{company_id}/{invoice_number}.pdf`); the code is internally contradictory about whether that URL is even publicly openable. Exhibitor download is auth-gated but ultimately returns the same raw URL. Current admin emails sidestep this by **attaching the PDF bytes** rather than linking.

**Net:** item #7 is effectively unbuilt, and building it naïvely would duplicate/conflict with 24.11 and 24.15 and lean on a non-existent secure invoice link. Hence the questions below.

---

## 3. Client questions (need product/client direction before build)

Each notes **why it's open** and **our recommendation** (for discussion, not a decision).

**CQ1 — Purpose & per-option availability of the dropdown.**
Two of the three options duplicate emails the system already sends automatically: "New Order Confirm/Invoice" already auto-sends at checkout, and "Cancelled Order" already auto-sends when an order is cancelled/refunded (24.11). So the manual dropdown is effectively a **re-send** control.
*Please confirm:* (a) that re-send is the intent (not a new/different email); and (b) whether each option should be **enabled only when it applies to the order's state** — e.g. "Cancelled Order" only for cancelled/refunded orders, "Confirmation" not for cancelled orders, "Reminder" only when a balance is due.
*Recommendation:* Yes to both — treat all three as re-sends, and gate each option on the relevant order state so an admin can't send a contradictory email.

**CQ2 — Scope of the manual payment reminder.**
The automated reminder covers **only split-payment, non-credit-card installments**. The manual option's scope is unspecified.
*Please confirm:* which orders qualify for a manual reminder — any order with an outstanding balance? credit-card installment plans too? one-time (non-plan) invoices?
*Recommendation:* Simplest is any product order with an outstanding balance, regardless of payment method.

**CQ3 — The invoice / payment link ("with link").**
"Invoice/Payment Reminder with link" and "New Order Confirm/Invoice" imply an emailable link, but no secure emailable link exists today.
*Please confirm two things:*
1. Is "the link" a **pay-now / payment** link, or an **invoice-PDF** link?
2. How should it be delivered — a **secure, time-limited link** (needs to be built), or **attach the PDF** (works today, matches the current confirmation email)?
*Recommendation:* A secure, time-limited link for anything customer-facing; attach the PDF as an interim if that can't make the release. A **permanent public link is not recommended** — the current invoice URL is guessable and never expires, so anyone with the URL (forwarded or guessed) could view the invoice's billing details with no login.

**CQ4 — Email-only confirmation.**
The system is email-only today (no SMS provider integrated) and the spec item is titled "Send Order **Email**."
*Please confirm:* email-only for this feature, SMS out of scope until the SMS-provider story lands.

---

## 4. Implementation decisions (our call / already decided — for information, not client questions)

These were pulled out of the client list. Decisions marked **DECIDED** are settled on our side; **OUR CALL** = our recommendation to build unless the client objects; **DEFERRED** = revisit after the client answers §3.

- **ID1 — Template dropdown = fixed, curated 3-option list** (not registry-driven / not surfacing admin custom templates). **DECIDED.** *(was Q1)*
- **ID2 — "New Order Confirm/Invoice" applies to product/booth orders only** (PPL/subscription confirmations stay in the PPL module). **DECIDED.** *(was Q2)*
- **ID3 — No preview/edit before send** — managed-template send; bodies are centrally owned by the Email & SMS epic. **DECIDED.** *(was Q3)*
- **ID4 — Recipients follow the existing pattern:** `billing_email` + the order's additional-emails CC list, with the primary-exhibitor fallback when billing email is missing. **DECIDED.** *(was Q11)*
- **ID5 — Audit + throttle every manual send** (who / what / when / to whom on Order Details; throttle to prevent accidental spamming) — standard, just build it. **DECIDED.** *(was Q13)*
- **ID6 — Manual reminder uses a distinct slug (or bypasses the cron dedupe)** so a manual send and the automated cron reminder don't silence each other. **OUR CALL.** *(was Q7)*
- **ID7 — Single `orders.send_email` permission to start;** split per-template only if the client later wants differentiated control. **OUR CALL.** *(was Q14)*
- **ID8 — Duplicate-send handling (idempotency / "last sent" indicator):** **DEFERRED** — decide after the client confirms CQ1–CQ3 (purpose, scope, availability), since the answers determine whether a re-send is a normal action or something to guard against. *(was Q5)*
- **ID9 — "Send Email" is a standalone action, with no coupled "save order."** The spec's "save order or send email" resolves to send-only; persisting billing edits stays the separate existing order-update endpoint (`PATCH /orders/:id`). **DECIDED.** *(was Q12/CQ4)*

---

## 5. Out of scope for this question set

24.6 System Spec items **#9 Sales Rep / Deal Owner Assignment**, **#10 QuickBooks Force Sync**, and **#11 HubSpot Sync** are separate sub-items with their own open questions (e.g., the HubSpot deal-owner sync on reassignment, QuickBooks force-sync) — not covered here.

---

## 6. Evidence appendix (for internal validation, not for client)

- Manual send absent; only fixed resends: `admin-backend-api/src/admin/orders/orders.controller.ts` (`:533` resend-confirmation, `:566` resend-portal-password); `services/order-actions.service.ts:543` (`resendConfirmation`, PDF attached, throttled).
- Confirmation auto-sends at checkout: `booth_order_confirmation` fired on the checkout/webhook path (same slug the resend re-uses).
- Template registry: `admin-backend-api/prisma/schema.prisma` `NotificationTemplate` (~`:255`) keyed to `trigger_events.slug`; seeds in `src/database/seeds/notification-template.seeder.ts` + `trigger-event.seeder.ts`. All seeded rows `channel: EMAIL`.
- 24.11 cancel/refund email: `order-cancel.service.ts:245,378` (`send_notification` flag), `order-notification.service.ts` (slugs `order_canceled`/`order_refunded`; recipient chain `:253`); **no dedupe**.
- 24.15 reminders: `background-worker-service/src/jobs/payment-reminder/payment-reminder.service.ts` (cron; predicate `status=scheduled AND payment_type != credit_card`; recipient `billing_email` only; dedupe `alreadyRemindedWithin` `:195`); no link in body.
- Invoice URL unsigned/guessable: `admin-backend-api/src/common/helpers/s3.service.ts:73` (raw CloudFront URL); key `exports/invoices/{company_id}/{invoice_number}.pdf`; contradictory public/private signals in `upload.service.ts:87` vs `s3.service.ts:89`.
