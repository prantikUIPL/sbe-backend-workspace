# Story 24.5 — Quick Actions Per Order Item

**Epic:** Order Management

**Confluence:** [Order Management → Story 24.5](https://unifiedinfotech.atlassian.net/wiki/spaces/SBE/pages/3859742741/SBE+-+Admin+Panel#Order-Management) _(page section; Confluence has no per-requirement anchor)_

**Build Status:** 11 requirement(s) to implement here (filter Build Focus = 'Implement here'). 0 covered elsewhere, 2 out of scope.

**Implementation:** Not yet implemented — analysis/feasibility stage only; no branch or SBE ticket cut yet. Extended tracking format applied; per-requirement build status is ◻ Not started below.

## Summary

This story asks for a per-order Quick Actions menu in Admin Panel > Order Management exposing: open Upsell / Onsite Sale / Booth Build workflows (Booth Build gated to confirmed/signed orders + a Booth Build role); Download Agreement (Word + PDF); Download Order Invoice (PDF); Update Additional Users (admin adds emails, exhibitor sets permissions); Resend Order Confirm + Invoice; Resend Portal Password; and permission-based Log in to Customer Portal impersonation - with backend enforcement and logging. There is NO order/order-management module in any repo, so the whole admin endpoint surface is net-new, but re-verification against the actual code shows the underlying primitives are far more complete than previously assessed and most already live IN admin-backend-api: agreement download in BOTH PDF (buildSimplePdf) and Word (.doc) is fully built in the admin cart module; a generic mailer.sendFromTemplate plus seeded order-confirmation/welcome templates back the resend actions; admin impersonation already keys off company_id (finds the company's primary exhibitor, mints exhibitor JWTs, writes impersonated_by); the role/permission framework and audit/login logging are proven. Building the net-new order endpoints on these built primitives is therefore Deliverable for 10 of 13 requirements. Only Update Additional Users stays Partial (admin-add-emails is feasible via the invite primitive, but the 'permissions set by exhibitor' piece has no built company-user permission model). Upsell and Onsite Sale workflows belong to other epics and are Out of Scope as link targets.

## Feasibility Counts

| Deliverable | Partial | Blocked | Not Deliverable | Out of Scope | Total | To Implement Here |
|---|---|---|---|---|---|---|
| 10 | 1 | 0 | 0 | 2 | 13 | 11 |

## Implementation Status (as of 2026-07-01)

**Branch:** — (not started). No SBE ticket / PR / Swagger tag yet.

No code has been written for story 24.5. The entire admin order-actions surface is net-new (there is no order/order-management module in any repo), and every in-scope requirement is ◻ Not started:

| Req | Status | Notes |
|---|---|---|
| 24.5-a | ◻ Not started | net-new `GET /api/v1/orders/:id/actions` per-order quick-actions endpoint |
| 24.5-b | Out of Scope — Upsell workflow (owned by Upsell / Order Management upsell epic) | link target only |
| 24.5-c | Out of Scope — Onsite Sale workflow (owned by Onsite Sale epic) | link target only |
| 24.5-d | ◻ Not started | Booth Build availability gating (status/agreement + role); link target owned by Booth Build Cart/Contract epic |
| 24.5-e | ◻ Not started | order-scoped `agreement.docx` reusing AgreementDocumentService |
| 24.5-f | ◻ Not started | order-scoped `agreement.pdf` reusing buildSimplePdf |
| 24.5-g | ◻ Not started | order `invoice.pdf` from written Invoice rows |
| 24.5-h | ◻ Not started | admin-add-emails feasible via invite primitive; 'permissions set by exhibitor' half has no built model (Partial) |
| 24.5-i | ◻ Not started | resend confirmation via mailer.sendFromTemplate |
| 24.5-j | ◻ Not started | resend portal password-reset email |
| 24.5-k | ◻ Not started | permission-based impersonation via company_id primitive |
| 24.5-l | ◻ Not started | per-action permission keys + gated actions set |
| 24.5-m | ◻ Not started | impersonation / password-reset logging |

**Remaining in story 24.5:** everything is remaining — all 11 in-scope requirements (24.5-a, 24.5-d, 24.5-e, 24.5-f, 24.5-g, 24.5-h, 24.5-i, 24.5-j, 24.5-k, 24.5-l, 24.5-m) are un-built. Note 24.5-h remains **Partial**: the admin-adds-emails half is deliverable, but the 'permissions set by exhibitor' half has no built company-user permission model.

**Team decisions still open (no code until answered):**
- **Booth Build gating** — confirm `confirmed` maps to `status = completed` and `signed` maps to the presence of an `OrderAgreement` row (OrderStatus has no literal 'confirmed'). Gates 24.5-d.
- **Resend Order Confirm and Invoice** — confirm whether the resend must ALSO attach the generated invoice PDF (vs merging the invoice number as text). Affects 24.5-i.

**Dependencies owned by other stories/epics (not 24.5 build work):**
- Upsell workflow (UI + backend) — needed for 24.5-b. (owner: Upsell epic — Order Management upsell)
- Onsite Sale workflow (UI + backend) — needed for 24.5-c. (owner: Onsite Sale epic)
- Booth Build Cart/Contract Creation workflow — the 24.5-d link target once the order is confirmed/signed. (owner: Admin Panel Booth Build Cart/Contract Creation Epic)

Front-end quick-actions menu wiring and anything already marked Out of Scope (the Upsell and Onsite Sale workflows) are outside API scope.

## Requirements

_Filter on **Build Focus** = `Implement here` to see only this story's real work._

| Req | Requirement | Verdict | Build Focus | Delivered By | Story Wording (verbatim) | Reason | Evidence | Dependency |
|---|---|---|---|---|---|---|---|---|
| 24.5-a | Surface a per-order Quick Actions menu in Admin Panel > Order Management, with the set of available actions computed/served by the backend per order. | Deliverable | Implement here | — | Each order shall offer quick actions: / Action availability and permissions shall be enforced on the backend. | No order/order-management module or controller exists in any repo, so the admin endpoint that lists per-order actions is net-new - but the Order model and every primitive it needs (status, agreement, company, billing, cart link) exist and are populated. Building the net-new actions endpoint on these built primitives is Deliverable. | admin-backend-api/prisma/schema.prisma:1540 (model Order, incl. company_id, status, cart_id); codebase_map KEY STRUCTURAL FACTS (no order module exists) |  |
| 24.5-b | Upsell quick action links to the Upsell workflow. | Out of Scope | Out of scope | — | Upsell links to the Upsell workflow. | The Upsell workflow is a separate epic; this story only opens/links to it. Grep confirms no upsell module/backend exists in admin-backend-api or exhibitor-backend-api. Only the action entry itself (covered by 24.5-a/l) is in scope. | none found (grep 'upsell' across admin/exhibitor src returns no module, only unrelated 'onsite' role/questionnaire hits) | Upsell workflow (owned by Upsell / Order Management upsell epic) |
| 24.5-c | Onsite Sale quick action links to the Onsite Sale workflow. | Out of Scope | Out of scope | — | Onsite Sale links to the Onsite Sale workflow. | The Onsite Sale workflow belongs to another epic; this story only links to it. Grep finds no Onsite Sale module - only an 'SBE Onsite Team' role and onsite-contact questionnaire fields, which are unrelated. | none found (grep 'onsite' hits role.seeder.ts:19 + product-questionnaire.seeder.ts only, no workflow module) | Onsite Sale workflow (owned by Onsite Sale epic) |
| 24.5-d | Booth Build quick action available only once the order is confirmed or signed, and accessible only to the Booth Build team role; links to the Booth Build workflow. | Deliverable | Implement here | — | Booth Build links to the booth build workflow, available once the order is confirmed or signed, accessible for the Booth Build team role. / Booth Build shall be available only once the order is confirmed or signed. | The availability gating THIS story owns is computable now: Order.status=completed represents the paid/confirmed state, OrderAgreement (one-per-order, written at checkout sign) represents 'signed', and the Permissions framework can gate to a Booth Build role. The link TARGET (Booth Build Cart/Contract Creation workflow) is owned by another epic (recorded as a cross-epic dependency), but the action-availability backend logic is Deliverable. | admin-backend-api/prisma/schema.prisma:1546 (status OrderStatus default pending), :1607 (model OrderAgreement, signed_at, @unique order_id); enum OrderStatus schema.prisma:421 (pending/partially_paid/completed/failed/refunded/canceled) | Booth Build Cart/Contract Creation Epic workflow (link target only) |
| 24.5-e | Download Agreement (Word / .doc) reflecting the order's booth details, pricing and terms. | Deliverable | Implement here | — | Download Agreement (Word) generates the contract document in .doc format including booth details, pricing, and terms as defined in an agreement. | PRIOR GAP DISPROVEN: a working Word (.doc) agreement generator already exists IN admin-backend-api - AgreementDocumentService.generate(id,'docx') emits application/msword HTML from live cart line items, pricing, fees and additional_terms, and is already wired to a download route. Orders are created from carts (Order.cart_id), so the net-new work is an order-scoped endpoint reusing this built generator. | admin-backend-api/src/admin/cart/services/agreement-document.service.ts:29 (generate pdf\|docx), :41-45 (.doc via buildWordHtml); admin/cart/cart.controller.ts:1141 (@Get :id/agreement.docx, @Permissions) |  |
| 24.5-f | Download Agreement (PDF) rendering the same agreement in PDF format. | Deliverable | Implement here | — | Download Agreement (PDF) renders the same agreement in PDF format. | PRIOR GAP DISPROVEN: the prior analysis claimed 'no PDF agreement renderer exists anywhere' - it does. admin-backend-api renders the agreement PDF via the dependency-free buildSimplePdf util, exposed today at the cart agreement.pdf route. Same content as the Word export. Net-new work is the order-scoped endpoint reusing the built PDF renderer. | admin-backend-api/src/admin/cart/utils/pdf.util.ts:49 (buildSimplePdf); agreement-document.service.ts:33-38 (pdf branch); admin/cart/cart.controller.ts:1122 (@Get :id/agreement.pdf) |  |
| 24.5-g | Download Order Invoice generates the invoice in PDF format reflecting the order's details. | Deliverable | Implement here | — | Download Order Invoice generates the invoice in PDF format. / The Word and PDF agreement and the PDF invoice shall reflect the order's details. | Invoice rows are actually written by the Stripe webhook for both PPL and booth orders (shared DB), a proven invoice-PDF renderer exists (exhibitor pdfkit renderInvoicePdf), and admin itself already renders PDFs (buildSimplePdf). The net-new work is an admin order endpoint that loads the order's Invoice rows and streams a PDF - Deliverable on built primitives. | Invoice written external-api-service/src/modules/webhook/services/webhook.service.ts:679,2048; renderer exhibitor-backend-api/src/ppl/services/ppl.service.ts:985 (renderInvoicePdf); admin PDF util pdf.util.ts:49; model Invoice schema.prisma:1962 |  |
| 24.5-h | Update Additional Users: admin adds portal-user email IDs (multiple users per account); access permissions are set by the exhibitor. | Partial | Implement here | — | Update Additional Users allows adding additional portal users; the admin shall add only the email IDs, and the access permission shall be set by the exhibitor; multiple users are supported under one account. | The admin-adds-emails half is feasible: the full invite primitive (create Exhibitor row + ExhibitorToken + send invitation email) is built and admin has the mailer to drive it (current invite is exhibitor primary-only, so an admin variant is net-new). The genuine gap is the 'permissions set by exhibitor' half - the company_user service exposes only invite/accept/resend/revoke/toggle-status; there is NO company-user permission/access-level model or endpoint for the exhibitor to set per-user permissions, so that capability is not built. | exhibitor-backend-api/src/company_user/company_user.controller.ts:49 (invite, primary-only), :254 (resend), :384 (list); company_user.service.ts:238 (exhibitor.create), :265 (exhibitorToken.create) - no permission-setting found in company_user.service.ts |  |
| 24.5-i | Resend Order Confirm and Invoice: send the order confirmation email with invoice to the customer. | Deliverable | Implement here | — | Resend Order Confirm and Invoice sends the order confirmation email with invoice to the customer. | Admin has a generic template dispatcher (mailer.sendFromTemplate, already used by resendWelcome) and the order-confirmation templates (ppl_order_confirmation, booth_order_confirmation) are seeded in admin. A net-new admin resend endpoint that composes order data and calls sendFromTemplate is Deliverable on built primitives. (Today these templates merge the invoice number as text - whether a PDF must additionally be attached is the one open question.) | admin mailer.sendFromTemplate admin-backend-api/src/admin/exhibitor-management/exhibitor-management.service.ts:448; templates admin-backend-api/src/database/seeds/notification-template.seeder.ts:93,142; existing send path external-api-service/src/modules/webhook/services/webhook.service.ts:1850,2531 (merges invoice_number) |  |
| 24.5-j | Resend Portal Password: send a reset-password email to the customer for portal access issues. | Deliverable | Implement here | — | Resend Portal Password sends a reset password email to the customer for portal access issues. | All primitives are built: admin already issues a fresh password + email via resendWelcome (mailer.sendFromTemplate + bcrypt + notificationLog dedupe), and the exhibitor portal has a full forgot/reset-password flow (token issue + reset email). A net-new admin order endpoint triggering a portal password reset/temp-password email is Deliverable on these built primitives. | admin resendWelcome exhibitor-backend-api... admin-backend-api/src/admin/exhibitor-management/exhibitor-management.service.ts:277 (resendWelcome), :448 (mailer.sendFromTemplate); exhibitor reset flow exhibitor-backend-api/src/auth/controllers/auth.controller.ts:294 (exhibitor-forgot-password) |  |
| 24.5-k | Log in to Customer Portal: permission-based impersonation of the customer, opening the portal view. | Deliverable | Implement here | — | Log in to Customer Portal allows permission-based impersonation of the customer, opening the portal view. | PRIOR 'PPL-only' CAVEAT CORRECTED: the impersonation primitive already keys off company_id - it finds the company's primary exhibitor (user_type=1), mints SBEEXHIBITOR access/refresh JWTs, is @Permissions-gated, and writes impersonated_by to the login log. An Order carries company_id, so the net-new admin order endpoint reuses this generic, built primitive directly. | admin-backend-api/src/admin/ppl-service-provider-overview/ppl-service-provider-overview.service.ts:320 (impersonate({company_id})), :339-341 (find primary exhibitor by company_id), :400 (signAsync JWTs), :431 (impersonated_by login log); controller :153 (@Get impersonate, @Permissions) |  |
| 24.5-l | Permissioned actions shown only to authorized roles; action availability and permissions enforced on the backend. | Deliverable | Implement here | — | with permissioned actions shown only to authorized roles. / Action availability and permissions shall be enforced on the backend. | A complete role/permission framework is built and used across admin (@Permissions decorator + guard, role-management, permission-management, seeders). New per-action permission keys are net-new config but the enforcement mechanism is proven on every existing admin controller. | @Permissions usage e.g. admin-backend-api/src/admin/exhibitor-management/exhibitor-management.controller.ts:100; admin/cart/cart.controller.ts:1123 (CART_PERMISSIONS.READ); permission/role seeders permission.seeder.ts, role.seeder.ts |  |
| 24.5-m | Impersonation and password reset shall be logged. | Deliverable | Implement here | — | Impersonation and password reset shall be permission-based and logged. | Logging primitives are built and already invoked: the impersonation service writes impersonated_by to exhibitor_login_log and emits logSecurity events; admin actions write adminAudit.record + logActivity (as resendWelcome already does). The new order endpoints record reset/impersonation events the same way. | impersonated_by login log ppl-service-provider-overview.service.ts:431; exhibitor-login-log.service.ts:20 (impersonated_by); adminAudit.record + logActivity exhibitor-management.service.ts:319-336 |  |

## Endpoints

| Method | Path | Purpose | Serves | Shared With |
|---|---|---|---|---|
| GET | `/api/v1/orders/:id/actions` | Return the permission- and status-gated set of quick actions for an order (Booth Build only when completed/signed and caller has the Booth Build role). | 24.5-a, 24.5-d, 24.5-l |  |
| GET | `/api/v1/orders/:id/agreement.docx` | Stream the order agreement as Word (.docx) reflecting booth details/pricing/terms. | 24.5-e | 24.6 — same agreement endpoint serves 24.6-s (view/download agreement). |
| GET | `/api/v1/orders/:id/agreement.pdf` | Stream the order agreement as PDF. | 24.5-f | 24.6 — same agreement endpoint serves 24.6-s (view/download agreement). |
| GET | `/api/v1/orders/:id/invoice.pdf` | Stream the order invoice PDF from the order's written Invoice rows. | 24.5-g |  |
| POST | `/api/v1/orders/:id/additional-users` | Admin adds one or more portal-user email IDs for the order's company (invitation sent; permissions set later by the exhibitor). | 24.5-h |  |
| POST | `/api/v1/orders/:id/resend-confirmation` | Resend the order confirmation email (with invoice details) to the customer. | 24.5-i |  |
| POST | `/api/v1/orders/:id/resend-portal-password` | Trigger a portal password-reset / temp-password email for the order's customer (logged). | 24.5-j, 24.5-m |  |
| POST | `/api/v1/orders/:id/impersonate` | Permission-based login as the order company's primary exhibitor (mint exhibitor portal JWTs), logged via impersonated_by. | 24.5-k, 24.5-m |  |

## Open Questions

- **[OPEN — for team discussion]** Booth Build gating: OrderStatus has no 'confirmed' value (pending/partially_paid/completed/failed/refunded/canceled). Confirm 'confirmed' maps to status=completed and 'signed' maps to the presence of an OrderAgreement row (which is written at checkout sign).
  - **Current direction (2026-07-01):** Lean toward mapping `confirmed` → `status = completed` and `signed` → presence of an `OrderAgreement` row (written at checkout sign), since there is no literal 'confirmed' status. Pending team confirmation before any gating code is written for 24.5-d.
- **[OPEN — for team discussion]** Resend Order Confirm and Invoice: the existing confirmation templates merge the invoice number as text - confirm whether the resend must ALSO attach the generated invoice PDF, which would require wiring the invoice-PDF renderer into the email.
  - **Current direction (2026-07-01):** — (pending) team to confirm whether the resend must attach the generated invoice PDF or whether the merged invoice-number text in the existing template suffices. Scope of 24.5-i depends on the answer.

## Cross-Epic Dependencies

- **Upsell workflow (UI + backend) to link/open from the quick action** — needed for 24.5-b (owner: Upsell epic (Order Management upsell))
- **Onsite Sale workflow (UI + backend) to link/open from the quick action** — needed for 24.5-c (owner: Onsite Sale epic)
- **Booth Build Cart/Contract Creation workflow to link/open once the order is confirmed/signed** — needed for 24.5-d (owner: Admin Panel Booth Build Cart/Contract Creation Epic)

## Build Consolidation Notes (avoid redundant work)

- **24.5-e** — [C6 cross-reference] OWNER — build here (GET /api/v1/orders/:id/agreement.pdf and /agreement.docx (r…); reused by other stories in this cluster.
- **24.5-f** — [C6 cross-reference] OWNER — build here (GET /api/v1/orders/:id/agreement.pdf and /agreement.docx (r…); reused by other stories in this cluster.
- **24.5-i** — [C11 build-once-reuse] OWNER — build here (MailerService.sendFromTemplate (renders a NotificationTempl…); reused by other stories in this cluster.
- **24.5-j** — [C11 build-once-reuse] OWNER — build here (MailerService.sendFromTemplate (renders a NotificationTempl…); reused by other stories in this cluster.
- **24.5-l** — [C8 build-once-reuse] Mostly delivered by building 24.1 (The existing JWT + roles guard + @Permissions(...) decorato…). Residual: per-action permission keys (e.g. Booth-Build role) + the actions GET that returns gated set.
