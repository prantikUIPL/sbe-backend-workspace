# Story 24.1 — View and manage all orders

**Epic:** Order Management

**Confluence:** [Order Management → Story 24.1](https://unifiedinfotech.atlassian.net/wiki/spaces/SBE/pages/3859742741/SBE+-+Admin+Panel#Order-Management) _(page section; Confluence has no per-requirement anchor)_

**Build Status:** 13 requirement(s) to implement here (filter Build Focus = 'Implement here'). 0 covered elsewhere, 1 out of scope.

**Implementation:** Not yet implemented — analysis/feasibility stage only; no branch or SBE ticket cut yet. Extended tracking format applied; per-requirement build status is ◻ Not started below.

## Summary

Story 24.1 asks for an admin Order Management list (table) showing all booth, add-on, booth-build, and sponsorship orders with columns for selection checkbox, Order ID + Customer Name (clickable to detail), Company Name, per-row quick links, Item Total, Balance Due, Show, Sales Person, Sales Channel, and Date, with Total/Balance Due "sourced from the centralized billing service" and list/access enforced on the backend. No order-management endpoint exists yet (the admin ppl-order list filters to subscription/ppl_addon and explicitly excludes product orders at ppl-order.service.ts:280), so the OM list API is net-new — but it sits on fully-built primitives: the Order model is written by admin checkout and exhibitor cart/payments with order_type=product, sales_person_id, cart_id, paid_amount (webhook-maintained), and items carry show_product_id. The story is overwhelmingly Deliverable: order_number, billing customer name, company, total, balance-due (total − paid_amount), show (order-item → show_product → show), date, sales channel (from cart.created_by_type + sales_person_id), and the Sales Person column (read of sales_person_id, which is both set at checkout AND admin-reassignable via exhibitor-strategist-assignment) are all backed by real write-paths. Two items fall short — "booth build" orders are explicitly deferred with no producer (Blocked), and the prescribed dedicated "centralized billing service" component does not exist though the figures are authoritative on Order columns (Partial). Quick Links belongs to the separate Quick Actions story (Out of Scope).

## Feasibility Counts

| Deliverable | Partial | Blocked | Not Deliverable | Out of Scope | Total | To Implement Here |
|---|---|---|---|---|---|---|
| 11 | 1 | 1 | 0 | 1 | 14 | 13 |

## Implementation Status (as of 2026-07-01)

**Branch:** — (not started). No SBE ticket / PR / Swagger tag yet.

Per-requirement build status (nothing built yet; all in-scope requirements remain ◻ Not started):

| Req | Status | Notes |
|---|---|---|
| 24.1-a OM list endpoint | ◻ Not started | net-new `GET /api/v1/orders` admin Order Management list |
| 24.1-b booth-build orders in list | ◻ Not started (Blocked — Booth Build feature (deferred) must ship an order-creation write-path before booth-build orders can appear.) | no booth-build order producer exists yet |
| 24.1-c Order ID + Customer Name cell | ◻ Not started | id + order_number + billing customer name in row DTO |
| 24.1-d Company Name column | ◻ Not started | company {id,name} join + billing_company_name fallback |
| 24.1-e Quick Links | Out of Scope — — | delegated to the Quick Actions Per Order story |
| 24.1-f Item Total column | ◻ Not started | `Order.total` stored value |
| 24.1-g Balance Due column | ◻ Not started | balance due = `total − paid_amount` |
| 24.1-h Show column | ◻ Not started | show name via orderItem → showProduct → show (multi-show open) |
| 24.1-i Sales Person column | ◻ Not started | `sales_person_id` (User join); blank for self-service |
| 24.1-j Sales Channel column | ◻ Not started | derived from cart.created_by_type + sales_person_id |
| 24.1-k Date column | ◻ Not started | `Order.created_at` |
| 24.1-l selection checkbox | ◻ Not started | selectable rows backed by row `id` |
| 24.1-m backend RBAC/access | ◻ Not started | `@Permissions` + JWT guard; seed OM permission keys |
| 24.1-n Total/Balance Due source | ◻ Not started | figures authoritative on `Order.total`/`paid_amount`; no standalone centralized billing service (Partial) |

**Remaining in story 24.1** (all in-scope requirements — nothing built yet):

1. **24.1-a** — OM list endpoint (`GET /api/v1/orders`).
2. **24.1-b** — include booth-build orders (**Blocked** — no producer; see Deferred Work).
3. **24.1-c** — Order ID + Customer Name clickable cell.
4. **24.1-d** — Company Name column.
5. **24.1-f** — Item Total column.
6. **24.1-g** — Balance Due column.
7. **24.1-h** — Show column (multi-show behavior pending — see Open Questions).
8. **24.1-i** — Sales Person column.
9. **24.1-j** — Sales Channel column.
10. **24.1-k** — Date column.
11. **24.1-l** — Per-row selection checkbox.
12. **24.1-m** — Backend-enforced list data / access (RBAC).
13. **24.1-n** — Total/Balance Due column-accuracy source (**Partial** — figures authoritative but no standalone billing service; see Open Questions).

**Team decisions still open (no code until answered):**
- **Multi-show 'Show' column behavior** — how the single Show column renders for an order spanning multiple shows (join/list names, 'Multiple', or single-show scoping). Gates 24.1-h's column semantics.
- **'Centralized billing service' — literal vs authoritative** — whether the spec mandates a standalone billing service as the source of truth or only authoritative figures. Gates 24.1-n's Partial resolution.

**Dependencies owned by other stories (not 24.1 remaining work):**
- **Booth Build order producer** — a write-path that creates booth-build orders — needed for 24.1-b (owner: Order Management / Booth Build (deferred feature)).
- **Quick Actions per order** — the per-row quick-action behaviour (24.1-e, Out of Scope) — owner: Quick Actions Per Order story; the list only returns the row `id`.
- **Order Details page** — the 24.1-c click destination — owner: separate OM Order Details story; 24.1 owes only the row `id`/`order_number`.

Front-end row-to-detail / action / checkbox wiring and anything already marked Out of Scope are outside API scope.

## Requirements

_Filter on **Build Focus** = `Implement here` to see only this story's real work._

| Req | Requirement | Verdict | Build Focus | Delivered By | Story Wording (verbatim) | Reason | Evidence | Dependency |
|---|---|---|---|---|---|---|---|---|
| 24.1-a | Provide a backend Order Management list endpoint returning all booth, add-on, and sponsorship orders in one place (paginated table). | Deliverable | Implement here | — | Admin shall be able to view and manage all booth, add-on, **booth build**, and sponsorship **orders** in one place. / The Order Management list shall be a table with the columns above, the Order cell linking to Order Details, per-row quick actions, and Total and Balance Due sourced from the centralized billing service. | No order-management list endpoint exists: the only Order list (admin ppl-order) hard-filters order_type to [subscription, ppl_addon] and excludes product (its Swagger doc claiming 'all order_type values' is stale vs the actual where-clause), so the OM list is net-new. Booth/add-on/sponsorship are all order_type=product rows actually written by built checkout flows, and the ppl-order module proves the exact paginated/filtered/sorted list pattern over the Order model. Building GET /orders over these BUILT primitives is straightforward. | admin-backend-api/src/admin/ppl-order/ppl-order.service.ts:277-281 (where-clause excludes product), 86-119 (paginated list pattern); admin-backend-api/src/admin/cart/services/checkout.service.ts:249-274 (order_type=product create); exhibitor-backend-api/src/payments/services/payments.service.ts:186-190,637-641 (deriveOrderType→product) |  |
| 24.1-b | Include booth build orders in the list. | Blocked | Implement here | — | Admin shall be able to view and manage all booth, add-on, **booth build**, and sponsorship **orders** in one place. | Booth Build is explicitly deferred and no write-path produces a booth-build order; only the nullable parent_cart_id placeholder exists and there is no booth_build value in OrderType (only subscription/ppl_addon/product). The list cannot surface booth-build orders until that feature ships a producer; the list itself works for all other order types. | admin-backend-api/prisma/schema.prisma:2789 (parent_cart_id … 'Nullable placeholder (Booth Build deferred)'); admin-backend-api/prisma/schema.prisma:412-418 (OrderType has no booth_build) | Booth Build feature (deferred) must ship an order-creation write-path before booth-build orders can appear. |
| 24.1-c | Order cell shows Order ID + Customer Name and is clickable to open the order's Order Details page. | Deliverable | Implement here | — | Order: Order ID + Customer Name (clickable, opens the order's details page) Refer User Story Order Details / The Order cell shall open the order's details page. | order_number is a unique stored column and customer name is captured as billing_first_name/billing_last_name at checkout (both admin and exhibitor paths). The list can return id + order_number + customer name so the UI builds the link. The Order Details destination page is a separate OM story, but the row data needed to render and link the cell is fully present. | admin-backend-api/prisma/schema.prisma:1542 (order_number unique), 1553-1554 (billing_first_name/last_name); admin checkout.service.ts:251,264-265; exhibitor payments.service.ts:188,202-203 |  |
| 24.1-d | Company Name column (customer organization). | Deliverable | Implement here | — | Company Name (customer organization) | Order.company_id is a required FK to Company, and the ppl-order list already includes company {id,name} and falls back to billing_company_name. The identical join applies to the OM list. | admin-backend-api/prisma/schema.prisma:1543,1584 (company_id FK); admin-backend-api/src/admin/ppl-order/ppl-order.service.ts:56-58,341 (company include + billing_company_name fallback) |  |
| 24.1-e | Quick Links (per-row action buttons). | Out of Scope | Out of scope | — | Quick Links (action buttons) Refer User Story Quick Actions Per Order Item | The spec explicitly defers this to the separate 'Quick Actions Per Order' user story. The list endpoint only needs to return the order id so the UI can wire those actions; the actions themselves are not part of this story. | story spec 24_1.md:21 ('Quick Links (action buttons) Refer User Story Quick Actions Per Order'); no quick-action code found |  |
| 24.1-f | Item Total column (total order value). | Deliverable | Implement here | — | Total (total order value) | Order.total is a stored Decimal frozen at order creation by both checkout flows; the ppl-order list already returns total.toFixed(2). Directly available. | admin-backend-api/prisma/schema.prisma:1551 (total); admin checkout.service.ts:261 (total write); exhibitor payments.service.ts:196; ppl-order.service.ts:342 |  |
| 24.1-g | Balance Due column (outstanding payment). | Deliverable | Implement here | — | Balance Due (outstanding payment) | Balance due = Order.total − Order.paid_amount. paid_amount is a real running total maintained by the Stripe webhook on each succeeded payment transaction (full and split), bumping paid_amount and paid_in_full_at. Both columns are populated by built write-paths, so the value is computable now. | admin-backend-api/prisma/schema.prisma:1551,1573 (total/paid_amount); external-api-service/src/modules/webhook/services/webhook.service.ts:2090-2105 (paid_amount.add + paid_in_full_at) |  |
| 24.1-h | Show column (show name) for each order. | Deliverable | Implement here | — | Show (show name) | In-scope product orders carry OrderItems with show_product_id, which joins ShowProduct→Shows for the show name; the snapshot is written by checkout. Caveat: there is no direct Order.show_id and a Cart/Order is explicitly modeled 'across shows', so an order can reference multiple shows — the data join is fully built, but the single 'Show' column's behavior for multi-show orders is a product/UI decision (see open question). | admin-backend-api/prisma/schema.prisma:1940 (OrderItem.show_product_id), 2642,2653 (ShowProduct.show_id→Shows), 2782 (Cart 'built for a company across shows'); admin checkout.service.ts:283 (show_product_id snapshot) |  |
| 24.1-i | Sales Person column — shows the creating or admin-assigned salesperson; blank for self-service. | Deliverable | Implement here | — | Sales Person (assigned salesperson who created the order or was assigned by admin; blank for self-service) / Sales Person shall show the creating or assigned salesperson and shall be blank for self-service. | The column reads Order.sales_person_id (User join) and renders blank when null. The creating salesperson is captured at checkout (resolveSalesPersonId sets it only for Sales-Team cart owners, null for exhibitor/self-service and non-sales admins). The prior 'no order-level admin reassignment' gap is disproven: exhibitor-strategist-assignment.service.ts performs an admin-driven bulk reassignment of Order.sales_person_id (from→to strategist) with audit logging. Both the 'created' and 'assigned by admin' clauses are backed; the display simply reads the current value. | admin-backend-api/prisma/schema.prisma:1575,1593 (sales_person_id + salesPerson User relation); admin checkout.service.ts:355-370 (resolveSalesPersonId, null for exhibitor/non-sales); admin-backend-api/src/admin/exhibitor-strategist-assignment/exhibitor-strategist-assignment.service.ts:117-135 (admin reassigns Order.sales_person_id) |  |
| 24.1-j | Sales Channel column — Admin/Sales Person vs Self-Service Portal. | Deliverable | Implement here | — | Sales Channel (Admin role/Sales Person, or Self-Service Portal) / Sales Channel shall indicate Admin/Sales Person or Self-Service Portal. | Derivable for in-scope product orders, which always carry cart_id: Order.cart_id → Cart.created_by_type (admin\|exhibitor) gives self-service vs internal, and sales_person_id presence distinguishes Sales Person from a non-sales Admin. All three columns are populated by built flows. The spec groups labels as 'Admin role/Sales Person' vs 'Self-Service Portal', which this derivation fully supports. | admin-backend-api/prisma/schema.prisma:1576 (cart_id), 2736-2740 (CartOwnerType admin\|exhibitor), 2788 (Cart.created_by_type); admin checkout.service.ts:271 (cart_id write) |  |
| 24.1-k | Date column (order date). | Deliverable | Implement here | — | Date (order date) | Order.created_at is auto-set (@default(now())); the ppl-order list already returns created_at and supports created_at sorting and date-range filtering, reusable directly. | admin-backend-api/prisma/schema.prisma:1581 (created_at); ppl-order.service.ts:307-333 (date range), 346 (returns created_at) |  |
| 24.1-l | Per-row selection checkbox (multi-select orders). | Deliverable | Implement here | — | Checkbox (select order) | Purely a UI affordance backed by the order id the list already returns; any bulk operation triggered from the selection belongs to other stories. No backend gap for surfacing selectable rows. | admin-backend-api/prisma/schema.prisma:1541 (Order.id); ppl-order.service.ts:337 (list returns id) |  |
| 24.1-m | List data and access enforced on the backend (RBAC/permissions). | Deliverable | Implement here | — | List data and access shall be enforced on the backend. | The codebase has an established @Permissions decorator + JWT bearer guard pattern used by the analogous ppl-order controller; the new OM endpoint can register order-management permission keys (seeded like other modules) and enforce access server-side. | admin-backend-api/src/admin/ppl-order/ppl-order.controller.ts:30-36 (@Controller('ppl-orders'), @ApiBearerAuth('JWT-auth'), @Permissions('ppl-order.list')); admin-backend-api/src/database/seeds (permission seeding pattern) |  |
| 24.1-n | Total and Balance Due shall be sourced from the centralized billing service (column accuracy). | Partial | Implement here | — | Total and Balance Due shall be sourced from the centralized billing service. / Each column shall reflect the order's actual values. | The numeric values are accurate and available now from Order.total and Order.paid_amount (frozen at checkout / webhook-maintained), so the columns themselves can be delivered. However, no dedicated 'centralized billing service' component exists — pricing is computed by an in-process cart-pricing service and totals are frozen onto Order columns. If the spec literally requires a standalone billing service as the source of truth, that service is unbuilt; if it only requires authoritative figures, those exist. | admin-backend-api/prisma/schema.prisma:1551,1573 (total/paid_amount columns); codebase_map.md:379 (in-process cart-pricing.service.ts; no separate centralized billing service) |  |

## Endpoints

| Method | Path | Purpose | Serves | Shared With |
|---|---|---|---|---|
| GET | `/api/v1/orders` | Paginated Order Management list with RBAC; returns per-row id, order_number, customer & company name, item total, balance due, show name, sales person, sales channel, date. Row id opens Order Details. | 24.1-a, 24.1-c, 24.1-d, 24.1-f, 24.1-g, 24.1-h, 24.1-i, 24.1-j, 24.1-k, 24.1-l, 24.1-m, 24.1-n | 24.2, 24.3, 24.4 — same canonical list endpoint (filters/search/sorting are query params); 24.1 is the listing container and introduces NO separate route; row id consumed by 24.6 Order Details and 24.5 quick actions. |

## Open Questions

- **[OPEN — for team discussion]** An Order has no direct show_id and a Cart/Order is explicitly modeled 'across shows', so an order can reference multiple shows via its line items. How should the single 'Show' column behave for a multi-show order (e.g. join/list multiple show names, show 'Multiple', or is the OM list scoped so each order is effectively single-show)?
  - **Current direction (2026-07-01):** — (pending). The join (orderItem → showProduct → show) is fully built and can return one or many show names; leaning toward returning a deduped `shows[]` array so the UI decides between listing names and rendering 'Multiple', but the column's contract is a product/UI decision to confirm before coding 24.1-h.
- **[OPEN — for team discussion]** Is 'sourced from the centralized billing service' a literal architectural requirement (a standalone billing service must exist as the source) or simply a requirement that Total/Balance Due be authoritative? No such service exists today; the figures live on Order.total/paid_amount.
  - **Current direction (2026-07-01):** — (pending). Current leaning is that the figures on `Order.total`/`paid_amount` (frozen at checkout / webhook-maintained) are authoritative and sufficient; if the team confirms only authoritative values are required, 24.1-n is fully deliverable, whereas a literal standalone billing-service mandate would leave it Partial (service unbuilt).

## Deferred Work

- **Booth-build orders in the OM list (24.1-b)** — **Blocked/deferred**: no write-path produces a booth-build order (only the nullable `parent_cart_id` placeholder exists and `OrderType` has no `booth_build` value). The list itself works for all other order types (booth, add-on, sponsorship). Booth-build rows can only appear once the deferred Booth Build feature ships an order-creation producer; the OM list needs no change beyond surfacing that order_type once it exists. Decision that unblocks it: the Booth Build feature delivering a booth-build order write-path.

## Cross-Epic Dependencies

- **A Booth Build order producer (a write-path that creates booth-build orders)** — needed for 24.1-b (owner: Order Management / Booth Build (deferred feature))

## Build Consolidation Notes (avoid redundant work)

- **24.1-a** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster.
- **24.1-c** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster.
- **24.1-d** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster.
- **24.1-f** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster. | [C10 cross-reference] OWNER — build here (A single source-of-truth for money figures: balance due = O…); reused by other stories in this cluster.
- **24.1-g** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster. | [C10 cross-reference] OWNER — build here (A single source-of-truth for money figures: balance due = O…); reused by other stories in this cluster.
- **24.1-h** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster.
- **24.1-i** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster.
- **24.1-j** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster.
- **24.1-k** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster.
- **24.1-l** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster.
- **24.1-m** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster. | [C8 build-once-reuse] OWNER — build here (The existing JWT + roles guard + @Permissions(...) decorato…); reused by other stories in this cluster.
- **24.1-n** — [C1 build-once-reuse] OWNER — build here (GET /api/v1/orders — the single paginated/filterable/search…); reused by other stories in this cluster. | [C10 cross-reference] OWNER — build here (A single source-of-truth for money figures: balance due = O…); reused by other stories in this cluster.
